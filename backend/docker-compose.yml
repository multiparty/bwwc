version: '3.8'

services:
  bwwc-backend:
    container_name: bwwc-backend
    restart: always
    build: .
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DJANGO_ENV=development